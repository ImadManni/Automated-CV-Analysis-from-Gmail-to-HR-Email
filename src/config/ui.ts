/**
 * Option d'animation pour l'app bar (header).
 * Mettre à false pour désactiver l'animation d'entrée du header.
 */
export const HEADER_ANIMATION_ENABLED = true

/**
 * Défilement automatique des images du hero (sans clic sur les points).
 * Mettre à false pour désactiver la rotation automatique.
 */
export const HERO_AUTO_ROTATE_ENABLED = true

/** Délai entre chaque image du hero (ms). */
export const HERO_ROTATE_INTERVAL_MS = 5000

/**
 * URL du tableau de bord Grafana (métriques workflow, pipelines, etc.).
 * Remplacer par l’URL réelle de votre instance Grafana.
 */
export const GRAFANA_DASHBOARD_URL = 'https://grafana.com/grafana/dashboards/'
